@model IEnumerable<ComedorEstudiantil.Application.DTOs.GradoSeccionListaDTO>

@{
    ViewData["Title"] = "Grados y secciones";
}

<div class="container py-4">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
        <div>
            <h1 class="h3 mb-1">
                Grados y secciones
            </h1>

            <p class="text-muted mb-0">
                Administración de los grupos disponibles para estudiantes.
            </p>
        </div>

        <a asp-controller="GradoSeccion"
           asp-action="Crear"
           class="btn btn-primary">
            Crear grado y sección
        </a>
    </div>

    @if (TempData["MensajeExito"] is not null)
    {
        <div class="alert alert-success alert-dismissible fade show"
             role="alert">
            @TempData["MensajeExito"]

            <button type="button"
                    class="btn-close"
                    data-bs-dismiss="alert"
                    aria-label="Cerrar">
            </button>
        </div>
    }

    @if (TempData["MensajeError"] is not null)
    {
        <div class="alert alert-danger alert-dismissible fade show"
             role="alert">
            @TempData["MensajeError"]

            <button type="button"
                    class="btn-close"
                    data-bs-dismiss="alert"
                    aria-label="Cerrar">
            </button>
        </div>
    }

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover align-middle">
                    <thead>
                        <tr>
                            <th>Grado</th>
                            <th>Sección</th>
                            <th>Nombre completo</th>
                            <th>Estado</th>
                            <th class="text-end">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var gradoSeccion in Model)
                        {
                            <tr>
                                <td>@gradoSeccion.Grado</td>
                                <td>@gradoSeccion.Seccion</td>
                                <td>
                                    <strong>
                                        @gradoSeccion.NombreCompleto
                                    </strong>
                                </td>
                                <td>
                                    @if (gradoSeccion.Activo)
                                    {
                                        <span class="badge bg-success">
                                            Activo
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">
                                            Inactivo
                                        </span>
                                    }
                                </td>
                                <td class="text-end">
                                    <div class="d-flex flex-wrap justify-content-end gap-2">
                                        <a asp-controller="GradoSeccion"
                                           asp-action="Editar"
                                           asp-route-id="@gradoSeccion.IdGradoSeccion"
                                           class="btn btn-sm btn-outline-primary">
                                            Editar
                                        </a>

                                        <button type="button"
                                                class="btn btn-sm @(gradoSeccion.Activo ? "btn-outline-danger" : "btn-outline-success")"
                                                data-bs-toggle="modal"
                                                data-bs-target="#modalCambiarEstado"
                                                data-id-grado-seccion="@gradoSeccion.IdGradoSeccion"
                                                data-nombre="@gradoSeccion.NombreCompleto"
                                                data-activo="@gradoSeccion.Activo.ToString().ToLowerInvariant()">
                                            @(gradoSeccion.Activo ? "Desactivar" : "Activar")
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }

                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="5"
                                    class="text-center text-muted py-4">
                                    No existen grados y secciones registrados.
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="modal fade"
     id="modalCambiarEstado"
     tabindex="-1"
     aria-labelledby="tituloModalCambiarEstado"
     aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form asp-controller="GradoSeccion"
                  asp-action="CambiarEstado"
                  method="post">
                @Html.AntiForgeryToken()

                <input id="idGradoSeccionEstado"
                       name="id"
                       type="hidden" />

                <div class="modal-header">
                    <h2 class="modal-title fs-5"
                        id="tituloModalCambiarEstado">
                        Cambiar estado
                    </h2>

                    <button type="button"
                            class="btn-close"
                            data-bs-dismiss="modal"
                            aria-label="Cerrar">
                    </button>
                </div>

                <div class="modal-body">
                    <p id="mensajeCambiarEstado"
                       class="mb-0">
                    </p>
                </div>

                <div class="modal-footer">
                    <button type="button"
                            class="btn btn-secondary"
                            data-bs-dismiss="modal">
                        Volver
                    </button>

                    <button id="botonConfirmarEstado"
                            type="submit"
                            class="btn">
                        Confirmar
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const modal = document.getElementById("modalCambiarEstado");
            const campoId = document.getElementById("idGradoSeccionEstado");
            const mensaje = document.getElementById("mensajeCambiarEstado");
            const botonConfirmar = document.getElementById("botonConfirmarEstado");

            modal.addEventListener("show.bs.modal", function (event) {
                const boton = event.relatedTarget;
                const idGradoSeccion = boton.dataset.idGradoSeccion;
                const nombre = boton.dataset.nombre;
                const estaActivo = boton.dataset.activo === "true";
                const accion = estaActivo ? "desactivar" : "activar";

                campoId.value = idGradoSeccion;
                mensaje.textContent =
                    `¿Desea ${accion} el grado y sección ${nombre}?`;

                botonConfirmar.textContent =
                    estaActivo ? "Desactivar" : "Activar";

                botonConfirmar.className =
                    estaActivo
                        ? "btn btn-danger"
                        : "btn btn-success";
            });
        });
    </script>
}